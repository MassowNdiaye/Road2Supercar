import React from "react";
import ReactDOM from "react-dom/client";
import Footer from "./components/Footer";

const root = document.getElementById("footer-root");
if (root) ReactDOM.createRoot(root).render(<Footer />);
